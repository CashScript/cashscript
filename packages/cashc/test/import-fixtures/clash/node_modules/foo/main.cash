import "./lib/x.cash";
import "bar/y.cash";
import "lib/x.cash";

contract NodeModulesInsidePackage() {
    function spend(int v) {
        require(localCheck(v) + viaBar(v) + packageCheck(v) > 0);
    }
}

function divmod(int a, int b) returns (int, int) {
    return a / b, a % b;
}

contract GlobalFunctionMultiReturn() {
    function spend(int x) {
        int q, int r = divmod(x, 3);
        require(q == 4);
        require(r == 1);
    }
}

contract Test() {
    internal function square(int a) returns (int) {
        return a * a;
    }

    function spend(int x) {
        int y = square(x) + square(x);
        require(y == 18);
    }
}

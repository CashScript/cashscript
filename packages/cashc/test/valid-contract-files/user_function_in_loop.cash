contract Test() {
    function increment(int a) returns (int) {
        return a + 1;
    }

    function spend(int x) {
        int sum = x;

        for (int i = 0; i < 3; i = i + 1) {
            sum = sum + increment(i);
        }

        require(sum == x + 6);
    }
}

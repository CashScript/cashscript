contract Loopy() {
    function doLoop() public {
        int i = 0;

        do {
            i = i + 1;
        } while (i < tx.inputs.length);

        console.log(i);
        require(i > 2);
    }
}

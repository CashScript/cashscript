contract Test() {
    function spend(int a) {
        int x = 0;
        bytes head = 0x00;
        bytes tail = 0x01;
        if (a > 0) {
            x = a;
            (head, tail) = 0x1234.split(1);
        }
        require(tail == 0x34);
    }
}

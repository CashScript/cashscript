contract Test(int threshold) {
    function spend(int value) {
        require(isAboveThreshold_(value));
    }

    function isAboveThreshold_(int value) {
        require(value > threshold);
    }
}

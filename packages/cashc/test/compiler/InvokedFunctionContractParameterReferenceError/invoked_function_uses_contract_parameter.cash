contract Test(int threshold) {
    function spend(int value) public {
        require(isAboveThreshold(value));
    }

    function isAboveThreshold(int value) internal {
        require(value > threshold);
    }
}

contract Test(int requiredValue) {
  function spend(int value) public {
    require(wrapper(value));
  }

  function wrapper(int value) internal {
    require(matchesRequiredValue(value));
  }

  function matchesRequiredValue(int value) internal {
    require(value == requiredValue);
  }
}

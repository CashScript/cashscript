contract Test() {
    function spend(int value) public {
        require(first(value));
    }

    function first(int value) internal {
        require(second(value));
    }

    function second(int value) internal {
        require(first(value));
    }
}

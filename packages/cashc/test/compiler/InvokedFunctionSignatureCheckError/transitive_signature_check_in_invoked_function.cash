contract Test(pubkey owner) {
  function spend(sig ownerSig) public {
    require(wrapper(ownerSig));
  }

  function wrapper(sig ownerSig) internal {
    require(checkOwner(ownerSig));
  }

  function checkOwner(sig ownerSig) internal {
    require(checkSig(ownerSig, owner));
  }
}

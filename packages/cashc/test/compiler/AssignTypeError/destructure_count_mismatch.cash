function pair(int a) returns (int, int) {
    return a, a + 1;
}

contract Test() {
    function spend(int x) {
        int p, int q, int r = pair(x);
        require(p == q);
        require(q == r);
    }
}

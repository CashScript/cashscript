pragma cashscript ^0.13.0;

import "./parity-helpers.cash" as Parity;

library MathHelpers {
    function isPositiveEven(int value) internal {
        require(value > 0);
        require(Parity.isEven(value));
    }
}

contract Test(int x, string y) {
    int globalInt = 400 + x;
    bytes b = 0x07364897987fe87;

    function hello(sig s, pubkey pk) {
        int myVariable = 10 - 4;
        int myOtherVariable = globalInt + myVariable % 2;
        require(myOtherVariable > globalInt);

        require(ripemd160(b) == ripemd160(y));
    }
}
